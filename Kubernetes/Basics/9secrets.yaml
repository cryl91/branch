apiVersion: v1
kind: Secret
metadata:
  name: Secret-info
data:
  username: abc
  password: xyz #Encode using base64 and paste the value here 
---
apiVersion: v1
kind: Pod
metadata:
  name: envsecrets
spec:
  containers:
  - name: nginx
    image: nginx:1.14.2
    env:
    - name: course
      valueFrom:  
        secretKeyRef:
          name: Secret-info
          key: username
    - name: Person
      valueFrom: 
        secretKeyRef:
          name: Secret-info
          key: password
#To get all secrets at once: 
    # envFrom: 
    # - secretRef:
    #     name: Secret-info