apiVersion: v1
kind: Pod
metadata:
  name: resources
spec:
  containers:
  - name: nginx
    image: nginx:1.14.2
    resources: 
      requests: 
        cpu: "100m"
        memory: "128Mi"
      liimts:
        cpu: "200m"
        memory: "256Mi"  
